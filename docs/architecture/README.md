# Architecture

The governing document is `2026-07-28-harness-architecture.md`, a dated snapshot of the workspace spec at `../../docs/superpowers/specs/`. New spec revisions are copied here with their date prefix; never edit snapshots in place.

Subsystem ownership documents (session ownership, prompt compiler boundaries, world-state contract) live here as they are written.
